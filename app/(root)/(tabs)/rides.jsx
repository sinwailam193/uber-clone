import { SafeAreaView } from "react-native-safe-area-context";
import { Text } from "react-native";

export default function TabRides() {
    return (
        <SafeAreaView>
            <Text>Rides page</Text>
        </SafeAreaView>
    );
}
